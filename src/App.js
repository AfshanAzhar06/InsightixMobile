// app.js
import 'react-native-gesture-handler'; // only if using gestures anywhere
import 'expo-dev-client'; // optional, only if using dev-client features

export default function App() {
  return null; // Expo Router handles navigation automatically
}
